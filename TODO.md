# TODO - Separar backend e frontend

- [ ] Entender a estrutura atual do projeto (feito via leitura de package.json e arquivos principais).
- [ ] Criar pastas `frontend/` e `backend/` na raiz.
- [ ] Mover os arquivos do Next.js (app/, components/, public/, etc.) para `frontend/`.
- [ ] Ajustar `package.json`, `next.config.mjs`, `tsconfig.json` e paths/aliases para o novo layout.
- [ ] Criar `backend/` com estrutura inicial (ex: Node/Express ou apenas placeholder) e `package.json` do backend.
- [ ] Atualizar README com instruções de como rodar frontend e backend.
- [ ] Testar build/dev do frontend para garantir que tudo continua funcionando.

