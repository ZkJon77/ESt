# backend

API de exemplo (Express) para separar front e back.

## Rodar

1. Instale deps:
   - `npm install`
2. Suba o servidor:
   - `npm run dev`

## Endpoints
- `GET /health`
- `POST /api/auth/login` (placeholder)

